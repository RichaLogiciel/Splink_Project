// Report routes have been removed - report generation is now handled by Lambda function
// See lambdas/order-summary-report/ for the Lambda implementation

import { Router } from "express";

const router = Router();

// All report routes removed - handled by Lambda function

export default router;
